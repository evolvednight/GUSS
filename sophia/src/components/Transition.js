import React, { Component } from "react";
import { Button } from "react-bootstrap";

class Transition extends Component {

    render() {
        return (
            <div>
                <Button onClick="none">Next</Button>
            </div>
        )
    }

}

export default Transition;